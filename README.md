# AAU P2 Article

## Compilation

### JetBrains IDEs

Run the `main` run configuration.

### Command Line

First, change the current directory to the `src/` directory

```shell
cd src/
```

and then run

```shell
latexmk
```
